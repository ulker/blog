
 {{include('inc/header.html.twig')}}
 <div class="container">
   <div class="row">
     <a href="{{absolute_url(asset(''))}}post/create" class="btn btn-primary">Gonderi Olustur</a>
   </div>
 </div>
<div class="posts">
	<table class="table table-striped table-hover ">
  <thead>
    <tr>
      <th>id</th>
      <th>Baslik</th>
      <th>Aciklama</th>
      <th>Islem</th>
    </tr>
  </thead>
  <tbody>
    {% for i in post %}
    <tr>
      <td>{{i.id}}</td>
      <td>{{i.title}}</td>
      <td>{{i.description}}</td>
      <td>
        <a href="{{ absolute_url(asset(''))  }}post/view/{{ i.id }}" class="btn btn-primary">Goruntule</a>
        <a href="{{ absolute_url(asset(''))  }}post/update/{{ i.id }}" class="btn btn-success">Duzenle</a>
        <a href="{{ absolute_url(asset(''))  }}post/delete/{{ i.id }}"  class="btn btn-danger">Sil</a>
      </td>
    </tr>
    {% else %}

    {% endfor %}
    
  </tbody>
</table> 
</div>
</body>
</html>